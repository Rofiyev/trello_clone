export interface ISiteConfig {
  title: string;
  description: string;
}

export const siteConfig: ISiteConfig = {
  title: "Taskify | @rof1yev",
  description: "Collabrote, manage projects, and reach new productivity peaks",
};
